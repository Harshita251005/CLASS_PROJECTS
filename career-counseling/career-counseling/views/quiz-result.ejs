<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Quiz Result</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body.result-body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(to right, #f4f4f4, #dff9fb);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .result-container {
      background-color: #ffffff;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      text-align: center;
      animation: fadeIn 1s ease-out;
    }

    .result-container h1 {
      font-size: 2rem;
      color: #2c3e50;
      margin-bottom: 20px;
    }

    .result {
      font-size: 1.5rem;
      color: #0984e3;
      font-weight: bold;
      margin-bottom: 30px;
      transition: transform 0.3s ease;
    }

    .result:hover {
      transform: scale(1.1);
    }

    .back-btn {
      text-decoration: none;
      background-color: #0984e3;
      color: #fff;
      padding: 12px 25px;
      border-radius: 8px;
      transition: background-color 0.3s ease;
    }

    .back-btn:hover {
      background-color: #74b9ff;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body class="result-body">
  <div class="result-container">
    <h1>Your Suggested Career</h1>
    <p class="result" id="careerText"><strong><%= career %></strong></p>
    <a href="/" class="back-btn">Go Back Home</a>
  </div>

  <script>
    // Add a little celebration confetti
    document.addEventListener("DOMContentLoaded", () => {
      const text = document.getElementById("careerText");
      text.style.opacity = 0;
      setTimeout(() => {
        text.style.transition = "opacity 1s ease-in-out";
        text.style.opacity = 1;
      }, 300);
    });
  </script>
</body>
</html>
